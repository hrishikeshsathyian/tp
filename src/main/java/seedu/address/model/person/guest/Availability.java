package seedu.address.model.person.guest;

/**
 * An enum that represents the availability of a guest. Has three values; ATTENING, NOT_ATTENDING, and PENDING
 */
public enum Availability {
    ATTENDING,
    NOT_ATTENDING,
    PENDING
}
